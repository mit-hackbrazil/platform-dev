# platform-dev
Platform to organize Teams, Tasks, Posts (Prototype Update) and Mentors.

# dependencies
- meteor
  mac/linux
  ```
  curl https://install.meteor.com/ | sh
  ```
  
# setup & running
```
cd app
meteor install
meteor run
```
http://localhost:3000
